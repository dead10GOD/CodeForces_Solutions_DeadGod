#include "bits/stdc++.h"
using namespace std;
using lli = long long int;
using vi = vector<int>;
int main()
{
    int n, h, g;
    cin >> n;
    vector<pair<int, int>> v;
    for (int i = 0; i < n; i++)
        cin >> v[i].first >> v[i].second;
    

    return 0;
}